import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { WebGLCanvas } from "@/components/three";
import { 
  Rocket, 
  TrendingUp, 
  Users, 
  Brain, 
  ArrowRight, 
  PlayCircle,
  FlaskConical,
  BarChart3,
  Sparkles
} from "lucide-react";

export default function Home() {
  return (
    <div className="space-y-16">
      {/* Hero Section */}
      <section className="relative overflow-hidden">
        {/* Three.js Animation Background */}
        <div className="absolute inset-0 z-0">
          <WebGLCanvas type="hero" className="w-full h-full" />
        </div>
        
        {/* Hero Content */}
        <div className="relative z-10 text-center space-y-8 py-20">
          <div className="space-y-4">
            <Badge variant="secondary" className="bg-accent-magenta/10 text-accent-magenta border-accent-magenta/20 backdrop-blur-sm">
              <Sparkles className="w-3 h-3 mr-1" />
              AI-Powered Startup Platform
            </Badge>
            <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-balance">
              Scale your startup with
              <span className="block text-accent-magenta">intelligent simulation</span>
            </h1>
            <p className="text-xl text-muted-foreground max-w-2xl mx-auto text-balance backdrop-blur-sm bg-background/50 rounded-lg p-4">
              SOL:GEN combines AI agents, predictive analytics, and collaborative tools to help startups 
              optimize their path to success through data-driven experimentation.
            </p>
          </div>
          
          <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <Button size="lg" className="bg-accent-magenta hover:bg-accent-magenta/90 text-white">
              <PlayCircle className="mr-2 h-5 w-5" />
              Start Simulation
            </Button>
            <Button variant="outline" size="lg" className="backdrop-blur-sm bg-background/50">
              <ArrowRight className="mr-2 h-4 w-4" />
              Explore Platform
            </Button>
          </div>
        </div>
      </section>

      {/* Features Grid */}
      <section className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        <Card className="border-accent-magenta/20 hover:border-accent-magenta/40 transition-colors">
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-accent-magenta/10 flex items-center justify-center mb-2">
              <Brain className="h-6 w-6 text-accent-magenta" />
            </div>
            <CardTitle>AI Agent Simulation</CardTitle>
            <CardDescription>
              Advanced AI agents simulate market conditions, customer behavior, and competitive dynamics
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/experiments">
              <Button variant="ghost" size="sm" className="p-0 h-auto text-accent-magenta hover:text-accent-magenta/80">
                Explore Experiments <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-2">
              <BarChart3 className="h-6 w-6" />
            </div>
            <CardTitle>Performance Benchmarks</CardTitle>
            <CardDescription>
              Compare your startup metrics against industry standards and peer performance
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/benchmarks">
              <Button variant="ghost" size="sm" className="p-0 h-auto">
                View Benchmarks <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-2">
              <Users className="h-6 w-6" />
            </div>
            <CardTitle>Team Collaboration</CardTitle>
            <CardDescription>
              Real-time collaboration tools for distributed teams to optimize startup operations
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/collaboration">
              <Button variant="ghost" size="sm" className="p-0 h-auto">
                Start Collaborating <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-2">
              <FlaskConical className="h-6 w-6" />
            </div>
            <CardTitle>Smart Experiments</CardTitle>
            <CardDescription>
              Design and run controlled experiments to validate business hypotheses
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/experiments">
              <Button variant="ghost" size="sm" className="p-0 h-auto">
                Run Experiments <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-2">
              <TrendingUp className="h-6 w-6" />
            </div>
            <CardTitle>Growth Analytics</CardTitle>
            <CardDescription>
              Track effectiveness metrics and identify optimization opportunities
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/effectiveness">
              <Button variant="ghost" size="sm" className="p-0 h-auto">
                View Analytics <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-2">
              <Rocket className="h-6 w-6" />
            </div>
            <CardTitle>AI Assistant</CardTitle>
            <CardDescription>
              Get personalized recommendations and insights from your AI business advisor
            </CardDescription>
          </CardHeader>
          <CardContent>
            <Link href="/dashboard/assistant">
              <Button variant="ghost" size="sm" className="p-0 h-auto">
                Chat with AI <ArrowRight className="ml-1 h-3 w-3" />
              </Button>
            </Link>
          </CardContent>
        </Card>
      </section>

      {/* Channel Visualization Section */}
      <section className="space-y-8 py-12">
        <div className="text-center space-y-4">
          <h2 className="text-3xl font-bold">Visualize Your Channel Performance</h2>
          <p className="text-muted-foreground max-w-2xl mx-auto">
            See how your marketing channels perform in real-time with our interactive orbital diagram. 
            Each dot represents a channel, sized by meetings per day and colored by payback efficiency.
          </p>
        </div>
        
        <div className="bg-card rounded-lg border border-border p-6">
          <WebGLCanvas type="channel-diagram" className="w-full h-[400px]" />
        </div>
      </section>

      {/* CTA Section */}
      <section className="text-center py-12 border-t border-border">
        <div className="space-y-4">
          <h2 className="text-3xl font-bold">Ready to optimize your startup?</h2>
          <p className="text-muted-foreground max-w-md mx-auto">
            Join thousands of entrepreneurs using SOL:GEN to accelerate their path to success.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Button size="lg" className="bg-accent-magenta hover:bg-accent-magenta/90 text-white">
              Get Started Free
            </Button>
            <Button variant="outline" size="lg">
              Schedule Demo
            </Button>
          </div>
        </div>
      </section>
    </div>
  );
}
